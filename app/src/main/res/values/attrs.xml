<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Color for a boolean status icon; true: this color; false: transparent. -->
    <attr name="checked_icon_tint" format="reference|color" />

    <!-- Color for a boolean status icon; true: this color; false: transparent. -->
    <attr name="completed_icon_tint" format="reference|color" />

    <!-- drawable/border.xml uses this color. -->
    <attr name="border_color" format="reference|color" />

</resources>
